import React from 'react'
import {StyledSettings} from '../styles/StyledSettings';
function Settings() {
    return (
        <StyledSettings>
         s
        </StyledSettings>
    )
}

export default Settings
